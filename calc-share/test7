/*
// A Sample Program
// This is a temperature conversion program.
*/
int result;

int convert(int c, int a[]) {
       int q;
       int f;
       if (f > b) {
       		q[2*a+c/b] = a+b/c;
		while(j != (p * 27 + 1 / 6)){
			covert(aq[a+b], a, k/10);
			return 12+ p(1,2,3,a+q);
			break;
		}
	}
       else q[c/6+1-3*2] = 100;
       f = c - 9/5 * 32;
       ?;
       ;
$;
       return f+d*u;
}
void main(void) {
       int n;
       n = ?;
       result = n*convert(n);
       $ result;
}
 
